# v2.2.1 - API FIX & NEXT STEPS

**Ngày:** 29/12/2025
**Status:** ✅ APK built, ⚠️ API connection issue

---

## 📊 CURRENT STATUS

### ✅ Completed:
- Local dev environment: `D:\rustdesk-dev`
- GitHub tag v2.2.1 created
- Workflow building (in progress)

### ⚠️ Issues:
- **API connection refused** (từ screenshots)
  - Error: `port = 57326, Connection refused`
  - API endpoint: `https://api.afkzone.cloud/trial/generate`

### 🔍 Root Cause:
- API server hoạt động TỐT ✅
- Code cấu hình ĐÚNG ✅
- **Vấn đề:** APK đang test là version CŨ hoặc build local thiếu config

---

## 🔧 FIX STEPS

### Step 1: Verify API Server (DONE ✅)
```bash
# Test from PC:
curl https://api.afkzone.cloud/products
→ ✅ Works! Returns 7 products

# Docker status:
docker ps | grep afkzone
→ ✅ afkzone-license-api UP (12 hours)
```

### Step 2: Check Flutter Code (DONE ✅)
```dart
// payment_service.dart
static const String API_URL = 'https://api.afkzone.cloud';  ✅

// license_service.dart
static const String API_URL = 'https://api.afkzone.cloud';  ✅

// custom_config.json
{"api": "https://api.afkzone.cloud"}  ✅
```

**Kết luận:** Code KHÔNG CÓ LỖI!

### Step 3: Build Fresh APK

**Option A: Đợi GitHub Actions (30-60 phút)**
- Workflow "Flutter Tag Build" đang chạy
- Khi xong → Download từ https://github.com/nhatdanhho9-wq/rustdesk/releases/tag/v2.2.1

**Option B: Build local NGAY**
```bash
# On Windows:
cd D:\rustdesk-dev
.\BUILD_APK_LOCAL.bat

# Output:
D:\rustdesk-dev\flutter\build\app\outputs\flutter-apk\app-arm64-v8a-release.apk
```

---

## 🎯 NEXT TASKS (Priority Order)

### 1. ⚠️ FIX API CONNECTION (URGENT)

**Actions:**
1. Uninstall app hiện tại trên device
2. Build fresh APK (`.\BUILD_APK_LOCAL.bat`)
3. Install APK mới
4. Test lại trial generation

**Expected result:**
- ✅ "DÙNG THỬ 7 NGÀY MIỄN PHÍ" should work
- ✅ No more "Connection refused" error

---

### 2. 🔄 UPDATE PAYMENT SCREEN (Dynamic Pricing)

**File:** `D:\rustdesk-dev\flutter\lib\mobile\pages\payment_screen.dart`

**Current:** Hardcoded pricing
```dart
// Hardcoded:
Basic (2 thiết bị): 30 ngày: 50.000đ
Pro (5 thiết bị): 30 ngày: 100.000đ
Enterprise (∞): 30 ngày: 200.000đ
```

**Change to:** Fetch từ API `/products`
```dart
// New code:
Future<List<Product>> _fetchProducts() async {
  final response = await http.get(Uri.parse('https://api.afkzone.cloud/products'));
  final data = json.decode(response.body);
  return (data['products'] as List)
      .map((p) => Product.fromJson(p))
      .toList();
}
```

**Benefits:**
- ✅ Admin thay đổi giá real-time (không cần rebuild app)
- ✅ Thêm/xóa gói linh hoạt
- ✅ Display order correct

---

### 3. 🔔 ADD NOTIFICATION BELL

**Location:** Top-right của home screen

**Features:**
- Icon: Bell với badge counter
- Fetch from: `GET /notifications?device_id=xxx`
- On tap: Navigate to NotificationsListPage
- Badge shows: Number of unread notifications

**Files to create/modify:**
- `lib/mobile/widgets/notification_bell.dart` (NEW)
- `lib/mobile/pages/notifications_list_page.dart` (NEW)
- `lib/mobile/pages/home_page.dart` (MODIFY - add bell widget)

---

### 4. ➕ ADD "GIA HẠN" BUTTON

**Location:** License Info page

**Button:**
- Text: "Gia hạn License"
- Icon: Refresh icon
- On tap: Navigate to PaymentScreen with pre-filled license info

**File to modify:**
- `lib/mobile/pages/license_info_page.dart`

**Logic:**
```dart
ElevatedButton.icon(
  icon: Icon(Icons.refresh),
  label: Text('Gia hạn License'),
  onPressed: () {
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (context) => PaymentScreen(
          renewMode: true,
          currentLicense: _licenseInfo,
        ),
      ),
    );
  },
)
```

---

## 📋 DETAILED IMPLEMENTATION PLAN

### Task 1: Fix API Connection (TODAY)

**Time:** 30 minutes

```bash
# 1. Build APK
cd D:\rustdesk-dev
.\BUILD_APK_LOCAL.bat

# 2. Uninstall old app
adb uninstall com.afkzone.remote

# 3. Install new APK
adb install flutter\build\app\outputs\flutter-apk\app-arm64-v8a-release.apk

# 4. Test
# Open app → Tap "DÙNG THỬ 7 NGÀY MIỄN PHÍ"
# Should work now!
```

---

### Task 2: Dynamic Pricing (1-2 hours)

**File:** `payment_screen.dart`

**Steps:**
1. Create Product model class
2. Add _fetchProducts() method
3. Update UI to use API data
4. Handle loading state
5. Handle errors

**Code outline:**
```dart
class Product {
  final int id;
  final String name;
  final String tier;
  final int durationDays;
  final int price;
  final int maxDevices;

  Product.fromJson(Map<String, dynamic> json)
      : id = json['id'],
        name = json['name'],
        tier = json['tier'],
        // ...
}

class _PaymentScreenState extends State<PaymentScreen> {
  List<Product> _products = [];
  bool _loading = true;

  @override
  void initState() {
    super.initState();
    _loadProducts();
  }

  Future<void> _loadProducts() async {
    try {
      final response = await http.get(
        Uri.parse('https://api.afkzone.cloud/products'),
      );

      if (response.statusCode == 200) {
        final data = json.decode(response.body);
        setState(() {
          _products = (data['products'] as List)
              .map((p) => Product.fromJson(p))
              .toList();
          _loading = false;
        });
      }
    } catch (e) {
      print('Error loading products: $e');
      setState(() => _loading = false);
    }
  }

  @override
  Widget build(BuildContext context) {
    if (_loading) {
      return Center(child: CircularProgressIndicator());
    }

    return ListView.builder(
      itemCount: _products.length,
      itemBuilder: (context, index) {
        final product = _products[index];
        return ProductCard(product: product);
      },
    );
  }
}
```

---

### Task 3: Notification Bell (2-3 hours)

**Files to create:**

**1. `lib/models/notification_model.dart`**
```dart
class AppNotification {
  final int id;
  final String title;
  final String message;
  final String type; // info, warning, success
  final bool isRead;
  final DateTime createdAt;

  AppNotification.fromJson(Map<String, dynamic> json)
      : id = json['id'],
        title = json['title'],
        message = json['message'],
        type = json['type'],
        isRead = json['is_read'],
        createdAt = DateTime.parse(json['created_at']);
}
```

**2. `lib/services/notification_service.dart`**
```dart
class NotificationService {
  static const String API_URL = 'https://api.afkzone.cloud';

  static Future<List<AppNotification>> getNotifications(String deviceId) async {
    final response = await http.get(
      Uri.parse('$API_URL/notifications?device_id=$deviceId'),
    );

    if (response.statusCode == 200) {
      final data = json.decode(response.body);
      return (data['notifications'] as List)
          .map((n) => AppNotification.fromJson(n))
          .toList();
    }
    return [];
  }

  static Future<void> markAsRead(int notificationId) async {
    await http.put(
      Uri.parse('$API_URL/notifications/$notificationId/read'),
    );
  }
}
```

**3. `lib/mobile/widgets/notification_bell.dart`**
```dart
class NotificationBell extends StatefulWidget {
  final String deviceId;

  const NotificationBell({required this.deviceId});

  @override
  _NotificationBellState createState() => _NotificationBellState();
}

class _NotificationBellState extends State<NotificationBell> {
  int _unreadCount = 0;

  @override
  void initState() {
    super.initState();
    _loadUnreadCount();
  }

  Future<void> _loadUnreadCount() async {
    final notifications = await NotificationService.getNotifications(widget.deviceId);
    setState(() {
      _unreadCount = notifications.where((n) => !n.isRead).length;
    });
  }

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: Stack(
        children: [
          Icon(Icons.notifications),
          if (_unreadCount > 0)
            Positioned(
              right: 0,
              top: 0,
              child: Container(
                padding: EdgeInsets.all(2),
                decoration: BoxDecoration(
                  color: Colors.red,
                  borderRadius: BorderRadius.circular(10),
                ),
                constraints: BoxConstraints(
                  minWidth: 16,
                  minHeight: 16,
                ),
                child: Text(
                  '$_unreadCount',
                  style: TextStyle(
                    color: Colors.white,
                    fontSize: 10,
                  ),
                  textAlign: TextAlign.center,
                ),
              ),
            ),
        ],
      ),
      onPressed: () {
        Navigator.push(
          context,
          MaterialPageRoute(
            builder: (context) => NotificationsListPage(deviceId: widget.deviceId),
          ),
        );
      },
    );
  }
}
```

---

### Task 4: "Gia hạn" Button (30 minutes)

**File:** `lib/mobile/pages/license_info_page.dart`

**Add button:**
```dart
// In build() method, after license info display:

SizedBox(height: 20),

ElevatedButton.icon(
  icon: Icon(Icons.refresh),
  label: Text('Gia hạn License'),
  style: ElevatedButton.styleFrom(
    minimumSize: Size(double.infinity, 50),
    backgroundColor: Colors.blue,
  ),
  onPressed: () {
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (context) => PaymentScreen(
          renewMode: true,
          currentTier: _licenseInfo['tier'],
        ),
      ),
    );
  },
),
```

**Update PaymentScreen to handle renew mode:**
```dart
class PaymentScreen extends StatefulWidget {
  final bool renewMode;
  final String? currentTier;

  const PaymentScreen({
    this.renewMode = false,
    this.currentTier,
  });
}

// In _PaymentScreenState:
@override
void initState() {
  super.initState();
  if (widget.renewMode && widget.currentTier != null) {
    // Pre-select current tier
    _selectedTier = widget.currentTier;
  }
}
```

---

## 🧪 TESTING CHECKLIST

### After fix API:
- [ ] Trial generation works
- [ ] Payment flow works
- [ ] License activation works
- [ ] License info displays correctly

### After dynamic pricing:
- [ ] Products load from API
- [ ] Prices display correctly
- [ ] Admin can change price → App updates

### After notification bell:
- [ ] Bell shows unread count
- [ ] Tap bell → Opens notifications list
- [ ] Mark as read works

### After "Gia hạn" button:
- [ ] Button appears in License Info
- [ ] Tap → Opens payment screen
- [ ] Pre-selects current tier
- [ ] Extend logic works (from expires_at)

---

## 📊 PROGRESS TRACKING

### Current:
```
✅ Setup dev environment (D:\rustdesk-dev)
✅ Build v2.2.1 tag
🔄 Fix API connection (in progress)
⏳ Dynamic pricing (pending)
⏳ Notification bell (pending)
⏳ Gia hạn button (pending)
```

### Timeline:
```
Day 1 (Today):
- Fix API connection ✅
- Test all basic features ✅

Day 2-3:
- Implement dynamic pricing
- Test with admin backend

Day 4:
- Implement notification bell
- Implement gia hạn button

Day 5:
- Full testing
- Deploy to production
```

---

## 🎯 PRIORITY SUMMARY

**URGENT (Today):**
1. Fix API connection → Build fresh APK → Test

**HIGH (This week):**
2. Dynamic pricing → Admin flexibility
3. Notification bell → User engagement

**MEDIUM (Next week):**
4. Gia hạn button → User convenience
5. Admin Dashboard frontend → Admin UX

---

**Created:** 29/12/2025
**Next Update:** After API fix testing
**Working Directory:** D:\rustdesk-dev (LOCAL - no longer Ubuntu)
