name: afkzone_ug_shell
description: UG-phone shell client (server-driven UI config MVP)
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  shared_preferences: ^2.3.2
  cryptography: ^2.7.0

flutter:
  uses-material-design: true

